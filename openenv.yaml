name: Smart Calendar Environment

description: RL environment for scheduling meetings efficiently

entry_point: smart_calendar_agent_environment:CalendarEnv

observation_space:
  type: dict

action_space:
  type: dict

reward_range: [0.0, 1.0]