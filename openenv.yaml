name: Smart Calendar Environment

description: RL environment for scheduling meetings efficiently

entry_point: server.smart_calendar_agent_environment:CalendarEnv

observation_space:
  type: dict

action_space:
  type: dict

reward_range: [-1.0, 1.5]
